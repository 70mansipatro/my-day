// Real authentication (register/login with JWT) will be implemented in Phase 2.

exports.register = (req, res) => {
  res.status(200).json({
    success: true,
    message: 'Coming in next phase',
  });
};

exports.login = (req, res) => {
  res.status(200).json({
    success: true,
    message: 'Coming in next phase',
  });
};
